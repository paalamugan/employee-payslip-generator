extend ./layouts/main.pug 

include ./components/title.pug 

block preheader
    | #{`Payslip for ${currentMonth}`}.

block content
    p
        span= t(`Dear `)
        b= employeeName
        | ,
    p.text-black= t(`We attached a ${currentMonth} payslip pdf below.`)